$fn = 60;

base_thick = 2.0;

//--------------------------------------------------------

rail_distance();

//--------------------------------------------------------

module rail_distance() {
	  w = 18;
	  h = 12;
	  difference() {
			cylinder(h=h, d=w);
			cylinder(h=h, d=4.2);
	  }
}

//--------------------------------------------------------
